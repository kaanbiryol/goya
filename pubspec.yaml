name: goya
description: yaml based style/theme manager for flutter apps.
version: 0.1.0
homepage: https://github.com/kaanbiryol/goya

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  yaml: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: