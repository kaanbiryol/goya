name: goya
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  yaml: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: